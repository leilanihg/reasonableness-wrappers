Web
===
Run run.sh and go to http://localhost:5000 from a Web browser on the same
machine.


Command-line
============
Run from the PARENT directory:
python3 -m primitives.representation a mailbox crossing the street
python3 -m primitives.representation a mailman crossing the street

For verbose output:
python3 -m primitives.representation -v a mailbox crossing the street
python3 -m primitives.representation -v a mailman crossing the street
